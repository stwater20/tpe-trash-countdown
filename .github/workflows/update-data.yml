{"name": "Update garbage truck data", "on": {"workflow_dispatch": {}, "schedule": [{"cron": "0 20 1 * *"}]}, "permissions": {"contents": "write"}, "jobs": {"update": {"runs-on": "ubuntu-latest", "steps": [{"uses": "actions/checkout@v4"}, {"uses": "actions/setup-node@v4", "with": {"node-version": 20}}, {"name": "Fetch data", "run": "node scripts/fetch-data.mjs"}, {"name": "Commit and push if changed", "run": "git config user.name github-actions[bot] && git config user.email github-actions[bot]@users.noreply.github.com && git add data/ && (git diff --cached --quiet && echo No changes || (git commit -m 'chore: update garbage truck data' && git push))"}]}}}
